package Estudos.GUI;

public class GUI_frames {
    public static void main(String[] args) {
        //JFrame frame = new JFrame(); // creates a frame
        //frame.setTitle("Jframe Title"); // creates a title of frame
        //frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // exit out of application
        //frame.setResizable(false); // prevents frame from being resize
        //frame.setSize(420,420); // defines the size of frame
        //frame.setVisible(true); // make the frame visible

        //ImageIcon image = new ImageIcon("confia.png"); // create an ImageIcon
        //frame.setIconImage(image.getImage()); // change icon of image
        //frame.getContentPane().setBackground(Color.green);
        //frame.getContentPane().setBackground(new Color(0xFFFFFF));

        //frame.getContentPane().setBackground(new Color(67,48,99));

        GUI_frame myframe = new GUI_frame();
    }
}
